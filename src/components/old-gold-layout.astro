---

import '../styles/global.css';
import {getCollection} from 'astro:content';
import {Image} from 'astro:assets';


import type { CollectionEntry } from 'astro:content';
const entries: CollectionEntry<'oldGold'>[] = (await getCollection('oldGold')).sort((a, b) => b.data.id - a.data.id);

---


<div class="image-collection-div">
    {entries.map((pic,index) => (
        <div class="image-entry">    
            <Image
                src={pic.data.image}
                alt={pic.data.type}
                width={pic.data.image.width}
                height={pic.data.image.height}
                loading={index === 0 ? "eager" : "lazy"}
                class="bg-image" />                
            <span class="link-details images hide-mobile"> {pic.data.type} </span>
        </div>       
    ))}
</div>

<p>{entries.length}</p>



<style>


</style>


